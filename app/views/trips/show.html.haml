%h2.text-center.pad2b= current_user.full_name
.container
  #trip-list.m1y
    = render partial: 'trip', locals: {trip: @trip}
.container
  .row.justify-content-center
    .col-md-6
      = link_to 'Edit', edit_trip_path(@trip), remote: true, class: "btn btn-secondary"
      #edit-trip-form
